name: DISP
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - cartopy>=0.18
  - cmocean
  - jupyter
  - jupyterlab
  - matplotlib
  - netcdf4
  - numpy
  - pandas
  - pytables
  - xarray

# To install new dependencies list them above
# and then type the following command
# conda env update --file environment.yml --prune
